// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("phpd", "for phpd support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("phpd", "enable phpd support", "no");

if (PHP_PHPD != "no") {
	EXTENSION("phpd", "phpd.c");
}

